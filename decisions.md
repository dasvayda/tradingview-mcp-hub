# Decisions

큰 방향만 적음. 할 일 목록은 [progress.md](progress.md). 실험 숫자·기각 파라미터는 [ledger.md](ledger.md).

최신이 위. 항목마다 **결정 / 하지 않음 / 이유** 를 유지함.

---

## 2026-09-17 — Keep을 일봉 MA 89로 올림

**결정**

- 새 Keep: `ma_len` 89, slow 100, reclaim 1.015, ATR×2, RR 2, cooldown 2.
- 테스터 같은 구간 NP **+93.4%**, PF 9.97, MDD 14%, 거래 **4**.
- 손익비는 3을 넘는 부분을 점수에 넣지 않음. 진 거래 0개인 PF 373이 수익을 이기지 못하게 함.

**하지 않음**

- reclaim 1.03 (거래 2, NP 59%, PF 373) 을 Keep으로 두지 않음.
- slow MA 150, lookback 14 를 새 기본값으로 두지 않음.

**이유**

- MA 89는 이전 Keep(+66%, 7거래)보다 수익·낙폭이 좋음. 거래는 더 적음.
- PF만 보면 2승 0패가 더 예뻐 보이지만, 번 돈은 더 적음.

**후속:** 거래 수 20 vs 4거래 Keep. 느슨한 진입으로 거래를 늘리지 않음.

---

## 2026-09-17 — Keep 주변 손절·목표·빠른 이평은 기각

**결정**

- 베이스는 그대로: reclaim 1.015, cooldown 2, ATR×2, RR 2, MA 50/100, invalid 0.985.
- 손절/목표 이웃(ATR 1.5·2.5, RR 1.5·2.5, invalid 0.97)과 빠른 이평(MA 34)은 다시 돌리지 않음.

**하지 않음**

- 거래 수 20을 맞추려고 진입을 쉽게 만들지 않음.

**이유**

- 넓힌 손절·높은 목표는 수익이 거의 사라짐. 좁힌 손절·낮은 목표는 NP가 66%에서 54~60%로 줄음. MA 34는 거래가 늘지만 낙폭 36%에 손실.

**후속:** 거래 수 목표를 일봉 전략에 맞게 낮출지. 그 전에는 `ma_len` 89 또는 `reclaim_pct` 1.03 만 후보.

---

## 2026-09-17 — 백테스트 기억은 ledger

**결정**

- 테스터 한 번은 `ledger.jsonl` 한 줄 + `ledger.md` 표로 남김. 날짜, git 커밋, 한 줄 변경, NP/PF/MDD/거래, keep 또는 reject.
- `runs/` 리포트는 그 날 로그일 뿐 gitignore. 다음 세션은 ledger 만 읽음.
- reject 의 `ban.match` 와 같은 파라미터는 허브 탐색이 다시 고르지 않음.

**하지 않음**

- 채팅 기억이나 `progress.md` 짧은 표만으로 실험을 추적하지 않음.

**이유**

- 재탈환을 느슨히 하거나 쿨다운을 줄인 결과가 채팅이 끝나면 흐려짐. 같은 실수를 코드가 다시 고르면 안 됨.

---

## 2026-09-17 — 리스크 규칙 2개 (재탈환 베이스)

**결정**

- 손절: 일봉 ATR × `atr_mult`, 이익 목표는 같은 ATR × `rr_ratio`.
- 무효: 새 일봉에서 하이킨아시 종가가 빠른 일봉 이평 × `invalid_pct` 아래로 가면 청산.
- 1시간 봉마다 거래하지 않음. 신호가 나와도 **날짜가 바뀐 봉**에서만 진입. 재진입은 `cooldown_days` 만큼 쉼.

**하지 않음**

- MACD, V패턴, 3분할 매도를 리스크 규칙으로 넣지 않음.

---

## 2026-09-15 — 베이스 가문: 일봉 이평 재탈환 (차트는 1시간)

**결정**

- 베이스 가문은 **일봉 이동평균 재탈환/되돌림** 하나임. 차트 TF는 1시간 유지. 일봉 HA·MA는 `request.security(..., "1D")` 로 가져옴.
- 첫 진입은 Daily MA 레퍼런스의 핵심만: 최근에 50일선(또는 100일선) 아래였다가, 일봉 양봉으로 그 선을 되찾거나 선에서 반등.
- 허브 템플릿을 새로 쓸 때 슬롯은 Long 하나. `HUB:` 손잡이는 이평 길이·근접 %·손절 정도만.

**하지 않음**

- 1시간 EMA 교차를 베이스로 두지 않음.
- Daily MA v1.0 전체를 복사하지 않음 (3분할, V패턴, MACD, 정배열 스택을 한 스크립트에 쌓지 않음).
- 평균 회귀만으로 1시간 횡보를 깎지 않음. DOGE 급등 한 방에 깨지기 쉬움.

**비교 (1시간 차트, `BINANCE:DOGEUSDT`)**

| 가문 | 1시간 성격과 맞음? | 테스터/표본 | 결론 |
|------|-------------------|-------------|------|
| 1시간 이평 교차 | 폭 1% 미만인 시간 65%에서 신호가 남 | 허브 스모크 거래 436, PF 0.96, NP ~-9% | 기각 |
| 일봉 이평 재탈환 | 느린 선이라 1시간 잡음을 덜 탐 | 같은 차트에서 Daily MA 테스터 PF 1.63, NP ~+20%, 거래 43, MDD ~9% | **베이스** |
| 1시간 V반등 | 10시간 창 조건이 봉의 2.8%만 해당 | Daily MA 안에 섞여 있음. 단독 표본 부족 | 기각 |
| 변동성 돌파 | 폭 3%+ 시간은 2.7%라 거래가 줄어듦 | 이 차트에서 아직 단독 성적 없음 | 보류 |
| 평균 회귀 | 횡보 65%에는 맞아 보임 | 시간당 최대 ~7% 급등. 숏/페이드가 한 방에 깨짐 | 기각 |

**이유**

- 디버그 재연결 후 테스터가 `Daily MA Strategy v1.0` 을 읽음: 순이익 비율 0.197 ≈ **+19.7%**, 손익비 **1.63**, 거래 **43**, 승률 비율 0.53 ≈ **53%**. 같은 1시간 차트에서 교차 템플릿보다 훨씬 나음.
- 그 성적은 일봉 HA 이평 때문이지, 1시간 EMA 교차 때문이 아님. 베이스는 그 **가문**만 가져오고 스크립트 전체는 가져오지 않음.

**후속:** 거래 수 20을 맞추려고 재탈환을 느슨히 하면 MDD가 35%를 넘음. 느슨한 진입으로 거래 수를 채우지 않음.

---

## 2026-09-15 — 허브 차트 시간봉은 1시간(`60`)

**결정**

- `config/default.yaml` 의 `timeframe: "60"` 을 유지함. 가문 비교·베이스 작성도 이 봉을 기준으로 함.
- Daily MA 레퍼런스는 `pinescript/reference/` 에 그대로 둠. 나중에 일봉 가문을 고르면 그때 차트를 `1D` 로 바꾸거나, `request.security(..., "1D")` 와 쿨다운·거래량 단위를 맞춰 고침.

**하지 않음**

- 지금 차트를 일봉으로 바꾸지 않음. 일봉으로 바꾸면 Daily MA 가문을 비교 전에 미리 고르는 셈이 됨.

**이유**

- 레이아웃 `crypto` 와 스모크가 이미 1시간임. 테스터: 거래 436, 승률 ~26%, 손익비 0.96, 순이익 ~-9%, 낙폭 ~31%.
- Binance `DOGEUSDT` 1시간 약 8000봉(2025-10-17 ~ 2026-09-15): 고저 폭 1% 미만인 시간이 **64.6%**. 3% 이상은 **2.7%**. 종가가 50이평 위인 시간은 **44%**. 1시간 교차가 횡보에서 과매매된 것과 맞음.
- 같은 심볼 일봉은 고저 폭 중앙값이 ~5.7% 이라 성격이 다름. 일봉 V조건(10봉 내 8% 하락+3% 반등)은 일봉 표본에서 너무 자주 맞아서, 차트 TF를 지금 바꾸면 가문 비교가 왜곡됨.

**후속:** [progress.md](progress.md) 의 DOGE 1시간 성격. 다음 할 일은 가문 표.

---

## 2026-09-15 — DOGE는 베이스 가문을 고른 뒤 최적화

**결정**

- 지금 템플릿(`doge_ema_rsi_atr.pine`)으로 `loop_limit` 파라미터 루프를 본 작업으로 돌리지 않음.
- DOGE 차트 성격과 전략 가문을 조사한 뒤, 진입 로직은 **가문 하나**를 베이스로 고름.
- 그 다음에만 테스터 루프로 파라미터·한 줄 수정을 함.
- 1차 목표는 승률이 아님. `config` 의 순이익% / 손익비 / 낙폭 / 최소 거래 수를 유지함.

**하지 않음**

- 여러 패턴(Daily MA + 이평 교차 + MACD + V반등 등)의 교집합을 한 스크립트에 쌓지 않음.

**이유**

- 스모크: 1시간 DOGE, 거래 436, 승률 ~26%, 손익비 0.96, 순이익 ~-9%. 이평 교차가 횡보에서 너무 자주 신호를 냄. EMA/RSI/ATR 숫자만 바꾸면 진입 종류가 그대로임.
- Daily MA(일봉 하이킨아시, 3분할)와 1시간 EMA 교차는 시간봉·진입 이유가 다름. 필터를 겹치면 과거 구간에만 예쁜 결과가 나오기 쉬움.
- 추세 추종은 승률이 낮아도 손익비가 되면 됨. 승률만 쫓으면 급등 구간을 놓치기 쉬움.

**후속:** [progress.md](progress.md) 다음 1~7.

---

## 2026-09-14 — 백테스트 점수는 TradingView 테스터만 사용

**결정**

- 로컬 백테스트 엔진을 만들지 않음. 주입·컴파일·성적 읽기는 tradesdontlie tradingview-mcp + Desktop CDP.

**하지 않음**

- 허브 안에서 캔들을 다시 계산해 전략을 채점하지 않음.

**이유**

- 목표가 “우리 시뮬”이 아니라 TradingView Strategy Tester 숫자임.
